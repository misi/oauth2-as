OAuth2Server\Entities\AuthorizationCodeEntity:
  type:             entity
  table:            oauth_authorization_codes
  repositoryClass:  OAuth2Server\Repositories\AuthorizationCodeRepository
  id:
    id:
      type:   integer
      generator:
        strategy: AUTO
  fields:
    code:
      type:       string
      max_length: 40
      unique:     true
    client_id:
      type:       integer
    user_id:
      type:       integer
      nullable:   true
    expires:
      type:       datetime
    redirect_uri:
      type:       string
      max_length: 200
    scope:
      type:       string
      max_length: 50
      nullable:   true
  manyToOne:
    client:
      targetEntity: OAuth2Server\Entities\ClientEntity
      joinColumn:
        name:                 client_id
        referencedColumnName: id
    user:
      targetEntity: OAuth2Server\Entities\UserEntity
      joinColumn:
        name:                 user_id
        referencedColumnName: id
